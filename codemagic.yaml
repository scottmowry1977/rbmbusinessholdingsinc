workflows:
  ios-testflight:
    name: RBM Holdings iOS TestFlight
    instance_type: mac_mini_m1
    max_build_duration: 30
    environment:
      groups:
        - app_store_credentials
      vars:
        XCODE_WORKSPACE: "ios/Runner.xcworkspace"
        XCODE_SCHEME: "Runner"
        BUNDLE_ID: "com.rbmbusinessholdingsinc.app"
      flutter: stable
      xcode: latest
      cocoapods: default
    integrations:
      app_store_connect: "RBM Connect"
    scripts:
      - name: Build and Sign IPA
        script: |
          # 1. Initialize the Mac keychain
          keychain initialize
          
          # 2. Fetch and create the signing files from Apple
          # Correct type for App Store/TestFlight is IOS_APP_STORE
          app-store-connect fetch-signing-files "$BUNDLE_ID" --type IOS_APP_STORE --create --install
          
          # 3. Add the certificates to the keychain
          keychain add-certificates
          
          # 4. Configure the project to use the new profiles
          xcode-project use-profiles
          
          # 5. Build the app
          flutter pub get
          # Note: we use xcode-project build-ipa as it is more robust for automatic signing
          xcode-project build-ipa --workspace "$XCODE_WORKSPACE" --scheme "$XCODE_SCHEME"
    artifacts:
      - build/ios/ipa/*.ipa
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
        beta_groups:
          - "Testers"
